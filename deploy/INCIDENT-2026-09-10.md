# September 10 reliability incident

Status: clean-run acceptance window running. Earlier acceptance windows are
retained as failed, ended, or impaired evidence. The execution-order release
passed engine verification at 2026-09-17T17:00:57Z, and the independent monitor
started the current 24-hour window at **2026-09-17T17:01:14Z** for run
`2026-09-17T16:54:41Z-3`. The September 15 directive removes forced SHADOW;
live execution remains subject to verified state and configured authorization
and risk checks.

At 04:34 UTC the actual engine process was alive, holding its kernel state lock,
on deployment `d13ce83f-19c0-4b13-bd04-b8c965361597`. The new incident was a
reconciliation outage, not a newly evidenced process crash. Current cgroup
`memory.events` showed zero OOM/OOM-kill events. That does not establish the
memory history of deleted containers.

## Preserved evidence and wipe scope

Target: project `f044176f-31ec-44a0-b6af-ae95824af6c0`, production environment
`d32e9a8b-4883-4ae1-92e1-66b50f8f7054`, service `live-hd1`
(`538c4ea3-74d6-4cea-bb5f-d5efb670439b`), `talented-volume`
(`c335ffee-4517-4134-bfb2-8a506e238894`) mounted at `/data`.

Pre-wipe snapshot: `.incident-private/2026-09-10/volume.tar.gz`, 552,580,206 bytes.
SHA-256: `b4eff5d5916bff7c04dd0a35b09e4a2b4e1a45910c9ce225a003934c99ce8196`.
The local off-volume copy was decoded, hashed, fully extracted, and checked for
unsafe archive entries. All four SQLite databases passed `integrity_check` and
have SQL dumps beside the extracted copies. The snapshot includes all three
state roots, complete retained ledgers/logs, embedded learned artifacts, source
snapshots, configuration, executable, and environment. Secrets remain in the
private ignored export; do not attach that directory to an issue or deployment.

The existing dirty source tree was backed up before edits. Git HEAD was
`73939ae`; the deployed image included uncommitted repairs. Its executable hash
was `491d5b62f9a2b6cce3c753adb79c8b4e0c7b7c5d7409e74d6ce13bae8552e33e`.
Deployment IDs and binary hashes are authoritative where a source commit is
unavailable. Full retained Railway logs and per-crash last-200-event selections
are in the private export. The 12:58 deployment retained only 50 total log rows;
missing events and historical OOM counters cannot be reconstructed.

New risk was paused before snapshotting. A 27.79-second planned SIGSTOP/SIGCONT
freeze made the raw files consistent; it is outside the acceptance run.
The archived execution ledger passed a two-pass economic replay audit.

The user reset/restored the same Railway service and volume at
**2026-09-15T19:28:25Z**, creating deployment
`869cb04b-f1f9-4466-a074-d3c1af6ef866` with an empty `/data`. The user confirmed
this action; the engine and recovery tooling did not issue a wipe. The bot's
separate volume was outside the reset scope. The interrupted September 15
archive download is incomplete and is **not** a recovery artifact.

At **2026-09-16T00:17:01.444Z**, the original September 10 archive was rehashed
against its creation-time checksum and bound to the same project, service,
volume and source deployment IDs. Only `live-account-identity.json`,
`live-submission-registry.json` and `live-trading-state.json` were restored.
The identity matched the execution account, signer and fresh exchange queries.
The complete archive remains off-volume. A local restore drill recovered all
43 scoped files byte-for-byte; this is not a production restore drill. The
reset receipt records the user reset and three individual file hashes.

Exchange reads at 04:38:30–33 UTC covered native perps and every returned builder
DEX. The positions were ORDI −4.02, MNT −22.9, AERO −61, xyz:CL −0.149,
mkts:US500 −0.041, and io:OAI +0.02. There was one external, reduce-only,
position-level TP on io:OAI (order `540750179113`, trigger 1829). Zero active
engine submissions is a separate fact from that exchange order. These reads
must be refreshed at the maintenance/reset boundary.

## Root causes and fatal-path audit

| Event UTC | Evidence | Cause and treatment |
| --- | --- | --- |
| Sep 9 12:58:02 | deployment `0cf973d0-7266-4af6-a3aa-fd8b39676103`, exit 1, `Authorization("Risk(...current_equity must be positive...)")` | Risk authorization propagated to the main fatal handler. Risk/authorization now reject an action, emit a structured alert, and keep reconciliation alive. Prior unified-equity repair remains in place. |
| Sep 9 20:12:43 | deployment `276c45d3-44cd-48a7-83d3-997547ed4fca`, exit 1, projected gross 231.329271 versus cap 231.2985705531342770 | Expected cap breach was escalated to a fatal engine error. All typed decision risk rejections now return NO_ACTION. Sent-order ownership is retained. |
| Sep 10 00:06:40.531 | MNT trade `39913972418164`, order `540750245820`, sell 45.8 from startPosition +22.9; exchange direction `Long > Short` | External reversal was rejected by reduction-only replay. Split it atomically into close 22.9/open short 22.9, allocate the single fee without duplication, close strategy attribution, and classify the new position as external. Never synthesize a strategy intent. |

The old wrapper replayed historical stderr at startup. Those repeated lines are
not additional crashes. The retained process journal records two historical
crashes and the expected September 9 rollback handover.

The audit searched exit/abort/panic/unwrap/expect sites and error propagation
through main, runtime, decision, signer, transport, reconciliation, state roots,
ledger, scheduler, streaming, and learning. Raw search inventories are preserved.
Categories:

| Boundary | Failure behavior |
| --- | --- |
| Decision risk, authorization, venue rejection | Reject action; structured reason and alert; retain any already-sent ownership. No flattening is invented. |
| RPC timeout, HTTP error, unknown receipt, position mismatch | Block execution, retain cursors, bounded retry, durable quarantine and measured recovery. |
| Config/schema validation, account identity, persistence/DB integrity | Log `engine_fatal`, return a failure ExitCode through normal runtime teardown. Wrapper preserves diagnostics; supervisor restarts paused. Never claim invalid data was repaired by restarting. |
| Invariant panic, poisoned lock, native library abort, OOM/process kill | Process supervision captures exit or unclean predecessor. Per-run diagnostics/cgroup evidence retained where available. Watchdog and Railway ALWAYS provide recovery; corruption can keep the child quarantined. |
| Supervisor/control setup or diagnostic persistence exception | Structured `supervisor_fatal` alert, explicit reason and failure status; Railway ALWAYS restarts the supervisor. No bare silent exit. |

Invariant assertions are retained rather than resuming a possibly corrupt writer.
Automatic restart mitigates an unexpected stop; it cannot make that stop count
as zero downtime. Any such event invalidates an acceptance window.

## Implemented controls

- The September 15 directive removes the blanket zero-size/shadow override.
  Reconciled, authorized orders use the existing risk projection and market cost
  gates. Initial boot blocks new risk until verification passes, then releases
  automatically. An explicit operator pause remains operator-controlled.
- A 120-second verified account watermark gates submissions. Reconciliation
  compares exchange positions/orders with the durable ledger and registry.
- Five bounded failure attempts use 30/60/120/240-second backoff, then one durable
  account-reconciliation DLQ item. Five-minute recovery probes retain the item;
  only successful account **and engine** convergence resolves it. Failed financial
  events are never discarded or skipped. Attempts, reason, lag and recovery time
  are exposed; nonempty DLQ emits an alert.
- Supervisor restarts the engine after 2 seconds initially, backing off to 30
  seconds. Every replacement is paused and reconciles on startup. A 180-second
  stale-snapshot watchdog requests shutdown; the grace period is 60 seconds.
  Single-writer lock, one replica, zero overlap and 90-second deployment drain
  remain active. Control telemetry runs outside the child engine.
- Stale values remain visible with source, age and reason. Their financial
  validity is explicitly rejected. Source signals show their own timestamps.
  The independent bot retains the last snapshot during endpoint outages.
- Independent bot samples every approximately 15–25 seconds; gaps over 45 seconds
  are recorded and alerted. Coverage sums measured adjacent intervals, never
  infers coverage from time since process start. Digests recur at 14 minutes plus
  polling latency; incident changes can alert immediately. Telegram transport
  failures and laptop/agent availability are not assumed away.
- Obsolete launcher backfill and the manual-restart-only policy were removed.
  The duplicate terminal monitor was reduced to read-only diagnostic commands.

## Public-RPC and SQLite gates (September 11)

The corrected benchmark uses the effective frozen **375-wallet** cohort. Its
493 admitted markets require HyperCore perp position/account reads, not ERC-20
balance calls. Multicall3 bytecode and chain 999 are pinned. Production batches
use two wallets below 551 markets and one above, with bounded payload, response,
gas, HTTP rate, and explorer fallback. Public market trades continue between
five-minute phased sweeps. The exact HyperCore block timestamp replaces the
quiet-period replay guard; a test with 1,000 continuous fills proves recovery
without waiting for an idle wallet.

Real-position probes returned all 1,496 rows for two wallets at 20M and 30M gas,
and 1,998 rows for three wallets at 30M. Production remains at two wallets with
strict per-call success, shape, numeric-width and freshness checks. No paid
endpoint was used.

The full-cohort audit finished at **2026-09-11T03:56:30.744Z**:

| Check | Observed result |
| --- | --- |
| Cohort / markets | 375 / 493 |
| Elapsed / EVM calls / explorer HTTP calls | 219.328 seconds / 190 / 0 |
| SQLite scan transactions / commit duration | 1 / 50 ms |
| Confirmed / recovering wallets | 375 / 0 |
| Persisted positions / tracked trade rows | 4,207 / 671 |
| New trade events during scan | 311 across 24 wallets; other 360 were subscription seed history |
| Wallet observation ages at database close | 0.702–217.023 seconds |
| Journal / integrity | WAL / integrity_check=ok |
| Orders submitted | 0 |

The scanner and trade updater share one connection with a 5,000ms busy timeout
and FULL synchronous durability. Scans stage all wallets outside a transaction,
fold in intervening trade updates, and commit the complete cohort once. A live
read during the scan observed trade commits before cohort completion. A separate
writer contention test and final-wallet fault injection verified lock waiting,
unblocked WAL reads, whole-scan rollback, retained trade data, and successful
retry. Position timestamps track actual changes; wallet timestamps track scans.
No position timestamp exceeded its wallet timestamp in the completed audit.

The boot checklist now requires a completed cohort transaction. The old JSON
backfill CLI, REST sweep scheduling branches, unreachable non-streaming daemon
paths, duplicate ABI decoding, and shutdown-time signal ingestion were removed.
Shutdown cancels outstanding read tasks before persisting state; it does not
turn late read results into new trading actions.

483 Rust tests and 28 bot tests passed, including process-boundary risk rejection,
logged shutdown, diagnostic-pipeline failure with independent stderr alert,
paused-boot timeout, and acceptance-window gap invalidation. These preflight
results do **not** start or complete the 24-hour production acceptance window.

## Learned-layer evidence and go/no-go

The pre-wipe model was epoch 1, trained through sample 64, with 240 completed
samples, 1,697,239 dropped learning samples, 10,832 explore decisions and zero
exploit decisions in retained counters. Most displayed candidates had negative
after-cost expectancy. These cumulative counts have different denominators;
do not present their ratio as a measured training-drop rate.

The degraded financial window is invalid for decisions. Clean-run accounting
starts at a verified baseline and distinguishes inherited exposure/manual
closures from new strategy evidence. A source reset or a zero-execution shadow
period is not evidence of improved financial performance.

The user authorized removal of the blanket shadow guard on September 15. This
changes execution capability; it is not evidence that the learned layer improved.
Positive after-cost edge, PF >1, Sharpe >=15 and ~3% rolling growth remain measured
targets. Runtime authorization, freshness, reconciliation, cost floors and risk
projection remain enforced. No new trade quota or profit guarantee is introduced.

## September 15 live-execution closeout

The blanket no-orders flag, zero-size override, hard-coded SHADOW status and
shadow-only verification requirement have been removed at the user's direction.
The production initializer now uses the same execution path covered by the live
runtime test. That test records one mock exchange fill after reconciliation and
rejects stale/unreconciled state before any order. No exchange order was placed
by validation. Normal cost, authorization and exposure limits remain active.

Boot verification gates new risk until it succeeds, then releases automatically;
manual pauses are not generated by supervisor restarts. A clean reset creates no
permanent pause. Rollback deliberately retains an operator pause for inspection.
The independent acceptance monitor requires NORMAL execution and no operator
pause. The removed shadow flag is not replaced by another permanent switch.

The SQLite schema now records each wallet's authoritative block number and
rejects regressing block/time updates. Legacy v1 state migrates without changing
its canonical hash; absent block provenance remains explicitly absent. The scan
audit compares every restored value and position count to RPC results plus trade
replay, then compares block numbers and timestamps across two complete scans.
`updated_at_ms` stays unchanged for unchanged position data.

The injected SQLite trigger exists only in a `cfg(test)` module using a temporary
database; there is no production switch. The release ELF was scanned: neither
that trigger nor the removed no-orders guard is present. Linux SHA-256:
`1fa10672b2eb09eb3354a65ba5a26a72e60308576687c528da66d531ef7112f1`.
480 Rust tests, 27 Python tests, all-target compilation, formatting and shell
validation passed. Obsolete non-streaming candle polling/ranking was removed;
the production Multicall/trade-stream path is unchanged by that cleanup.

The two-scan audit exposed a negative-zero checksum mismatch: the writer retained
`-0.000000` while Decimal parsing removed its sign. The reader now preserves
the exact stored sign and scale for every Decimal column, including zero. Hash
validation is unchanged. All zero scales 0–28 and both signs are covered.

The original performance reference is committed in `source-scan-baseline.json`.
The reproducible audit fails and emits `source_scan_regression` if a scan exceeds
190 EVM calls or 125% of 219.328 seconds. Row counts compare with current RPC
values, since positions and the listed market universe change over time.

The corrected two-scan validation passed for 375 wallets and 494 markets:

| Scan | EVM requests | Seconds | Cohort commits / ms | Verified positions | New trade updates | Recovering |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 190 | 224.908 | 1 / 41 | 4150 | 1129 | 0 |
| 2 | 188 | 219.502 | 1 / 52 | 4151 | 1557 | 0 |

Every persisted row matched decoded RPC plus replay. All 375 wallet blocks and
source timestamps advanced between scans; position change timestamps never
regressed. The results are saved in `source-scan-validation.json`. The production code change is net −142 lines versus `d89f9ef`, excluding test
modules and examples. With regression tests and audit examples included, the
code diff is +227 lines; safety evidence is not removed to force that total negative.

## September 16 recovery release

The first clean boot, deployment `062e3a3d-7c6a-46e4-9bf8-d3bd6fb325cb`, began
at 00:20:16 UTC and failed its 00:35:16 UTC verification deadline. It remained
alive in RISK_ONLY with a durable reconciliation DLQ entry; no acceptance clock
started. At 12:26 UTC the last available engine snapshot was 59 seconds old and
explicitly unverified. Independent observations confirmed the failure.

The gate exposed a real accounting defect: two authenticated manual AERO
increases (188 and 51) extended an existing strategy short from −61 to −300.
The ledger rejected mixed ownership as PositionDivergence. The repair preserves
cash, fees and exposure, permanently removes strategy ownership for the entire
shared episode, and moves source accounting to `recovered:unattributed`.
Append-only episode tombstones survive closure, persistence and compaction.
Attribution setters, source allocation and restore validation reject reclamation;
subsequent strategy fills cannot remove the marker. Legacy pure-manual episodes
receive tombstones during the versioned migration. Engine provenance updates
skip mixed episodes and their fills do not update realized strategy labels.

RISK_ONLY exit performs another complete live reconciliation **after** replay:
fills, funding, active-venue positions and open orders must converge again.
A successful recent read certificate is consumed by the transition; failed
reads invalidate it. A cached or repaired ledger alone cannot authorize exit.
Tests change exchange truth after the initial replay, verify exit is rejected
with no order, and require a new successful full read. Fatal integrity errors
still propagate to the logged controlled-shutdown boundary.

The pre-release exchange query at approximately 12:36 UTC still showed AERO
−300 and external reduce-only order `541191621726`, with trigger below 0.5096.
The order was left untouched. Replaying current authenticated history against
the saved ledger applied 9 external fills and 213 funding events; the second
pass applied zero. The engine's independent two-pass replay then matched AERO
−300 with 40 verified strategy fills, 310 funding records and 1674 registry
entries. Reconstructed historical equity is **not** clean-run performance.

The first daemon cohort on the failed boot committed 375 wallets and 495 markets
in one 174 ms transaction; 1087 trade updates had arrived concurrently. Source
polling is phased over 300 seconds in production. The standalone saved baseline
is 188 requests / 219.502 seconds, so phased daemon elapsed time must be reported
separately. The repaired release logs cumulative EVM requests and elapsed time
at every cohort commit to make that comparison observable.

Current process diagnostics and ownership files were captured off-volume before
the repaired deployment. The corrective release later verified at 18:57:04 UTC
on deployment `2df50a16-e123-415e-96ae-585691d397bb`, executable SHA-256
`dbb027612acc990b7e384531a492fb43822f43e821ec006b17bf7f539e65dff5`.
The first verified independent monitor observation started an acceptance window
at 18:57:15 UTC, but that window failed at 18:59:49 UTC after one bot control
read fell back to a retained snapshot. Bot deployment
`44ecd0c7-e2f7-4f13-a763-6464ce9c6d64` fixed that monitor brittleness without
restarting the engine. The 19:18:05 UTC window was then ended deliberately at
23:47:21 UTC by deployment `dc02bbe8-75cb-4800-88c8-b045ec06127e` after a
suspected negative-conservative Explore admission issue before first fill. That
interpretation was later corrected: bootstrap Explore with positive upper edge,
no actionable support and bound tail/explore caps is intentional. The deployed
binary SHA-256 was `770d6a3932a8112bbc5a2d5cd2da17395c2dc2bf2891b158de7a21f15caa80ac`.
The replacement boot verified at 23:53:43 UTC, and the independent monitor
started a new acceptance window at 23:53:53 UTC. Later releases invalidated that
and subsequent windows before any clean-run fill. Complete release,
verification and hourly evidence is recorded in `RUN-2026-09-10.md`; no 24-hour
run is inferred until real elapsed time is observed.


## September 17 readiness and freshness repair

The 2026-09-16T23:53:53Z acceptance window was invalidated before completion.
Live audits on September 17 showed that the engine was alive and reconciled, but
not fully capable of trading: the AERO asset-hold path could still surface as a
global `strategy_targets_unavailable` blocker, and MFCE status could expose
policy outputs older than the configured 75-second source freshness budget. That
made the window impaired rather than a clean selective no-trade period. The
recorded impaired time includes approximately 14h29m under the
global-blocker/stale-output state; the earlier 19:18Z Explore concern is now
recorded as a misdiagnosis rather than a model-safety violation.

Deployment `5c00602a-dda1-4570-8d99-1bafa6f29665` with engine SHA-256
`a7dbedfa2e6d86da545bda897b1695c50a30104034aebf159d6a747aba1a0253` makes the
AERO hold diagnostic explicitly asset-scoped, keeps global strategy readiness
enabled for asset-local holds, and filters MFCE policy outputs using the same
source freshness budget used by execution authorization. Regression coverage now
includes `stale_mfce_policy_output_is_neither_reported_nor_executable`, which
seeds a positive pending output, advances past the configured freshness budget,
and verifies that the output is absent from status and cannot authorize a live
intent.

The readiness/freshness boot verified at 2026-09-17T14:23:16Z. The independent
monitor started a window at 2026-09-17T14:23:30Z for run
`2026-09-17T14:12:56Z-3`. At the gate, execution was NORMAL, blockers were
empty, reconciliation lag was 2 seconds, DLQ depth was zero, active submissions
were zero, 375/375 source wallets were confirmed, recovery count was zero, the
first source commit took 188 ms, and no stale policy outputs were served.


## September 17 streamed-book execution-order repair

The 2026-09-17T14:23:30Z window was invalidated before any clean-run fill. Live
evidence showed the engine was healthy, reconciled and source-confirmed, but
still not submitting: policy outputs appeared in bursts, AERO repeatedly
superseded pending intents before fill, and the clean-run execution count stayed
zero. The root cause was in the streamed order-book hot path. For assets with an
active MFCE transition, a fresh book triggered MFCE recomputation before the
existing pending action reached `try_execute_pending`; if recomputation produced
a decision, execution was skipped. An active asset could therefore replace its
own pending intent indefinitely.

Deployment `6a8d79bf-fe26-4ed3-9d46-fa8644ebc7d5` with engine SHA-256
`37f46b2633b6b995476c510289ba339e84b4bb1d57689c8e86a00abb0f228bc0` gives an
existing pending action first right of refusal on the just-arrived order book,
then recomputes only when no intent was prepared or when execution explicitly
requires replanning. The regression
`streamed_book_executes_existing_mfce_pending_before_recompute` exercises the
streaming ingest path that direct `try_execute_pending` tests had missed. The
execution-first path still prices from the fresh book: `ioc_snapshot(book,
received_at)` supplies the evaluation midpoint, IOC limit, minimum-notional
validation, projected market rules and signed limit price.

The new boot verified at 2026-09-17T17:00:57Z. The independent monitor started
a window at 2026-09-17T17:01:14Z for run `2026-09-17T16:54:41Z-3`. That window
was invalidated before any fill when live all-reject evidence was traced to a
working-tree gate bug rather than a market with no edge: the binary rejected any
negative conservative edge before allocation, including cold-start Explore cases
whose upper edge was still positive.

## September 17 bootstrap Explore gate repair

The no-trade state after the execution-order release was a gate deadlock, not a
market-quality conclusion. Cold-start bootstrap outputs have `q50=0`,
`q10=-100` and `uncertainty=100`; after the configured friction buffer, the
sampled outputs showed `net_q50=-27`, conservative edge around `-127`, and
upper edge still positive. The correct invariant is scoped: exploit-sized risk
requires positive conservative/lower-tail evidence, while bootstrap Explore is
allowed when upper edge is positive, conditional support is not actionable, and
tail/explore caps bind. Requiring positive conservative edge for Explore made
"reject everything forever" a theorem and starved the learner of labels.

Deployment `ebc9e258-baed-48b1-8ca7-9d352c0d09b7` with engine SHA-256
`b097f3cca03652ef63dedd20ee896c3e8b1f8860c31bca03ef79a6c56e23b7ab` restores
the HEAD upper-edge semantics in `crates/mfce/src/lifecycle.rs` while retaining
the engine freshness and execution-order repairs. The verification set includes
`empty_engine_serves_a_conservative_exploration_prior`,
`pure_flow_can_explore_without_source_members_and_keeps_risk_limits`,
`streamed_book_executes_existing_mfce_pending_before_recompute`, and
`stale_mfce_policy_output_is_neither_reported_nor_executable`; the full release
build passed.

Live postdeploy evidence confirmed the deadlock was removed. At
2026-09-17T17:21:59Z, `/signals` showed 12 fresh Explore outputs and admitted a
HYPE bootstrap Explore probe at tiny notional with `net_q50=-27`, rather than
rejecting it as strong negative expectancy. The first source cohort committed
with 375/375 wallets confirmed and zero recovering; boot verification became
`VERIFIED` at 2026-09-17T17:25:53Z. The independent monitor started the current
acceptance window at 2026-09-17T17:26:15Z for run `2026-09-17T17:19:42Z-3`.
A post-gate sample at 2026-09-17T17:28:00Z had blockers empty, execution NORMAL,
source healthy, reconciliation lag 22 seconds, DLQ depth zero, one 266 ms cohort
commit, fresh policy outputs within 60 seconds, no process-start executions yet,
and one unresolved AERO pending action to keep watching for churn.

## Run acceptance and rollback

No clock starts on a degraded boot. Record the exact run/deployment/binary IDs,
verify account and source readiness, live process/lock, NORMAL execution mode, DLQ=0,
fresh telemetry and independent monitor observations first. Then record the
acceptance start UTC. Any unexplained stop, unverified-state order, monitoring
gap or unresolved reconciliation failure invalidates that window. Explore fills are
valid only under the scoped bootstrap-Explore invariant:
positive upper edge, no actionable conditional support, exchange-valid tiny
size, and bound tail/explore budgets. Exploit-sized learned allocation still
requires the conservative and lower-tail gates to clear. The run log must tag
Explore and Exploit separately so exploration labels cannot be confused with
full-size learned performance.

The hourly table is maintained in `RUN-2026-09-10.md`. Twenty-four real elapsed
hours must be observed; no forecast is a completed result.

Preauthorized rollback command from this checkout:

```sh
python3 scripts/recover_incident.py
```

It deploys maintenance, validates the exact volume and snapshot digest, refuses
to operate with an engine process present, restores the pre-wipe snapshot,
reapplies the pause, and redeploys the repaired image with an explicit operator pause.
It does not restore old signing bugs or blindly resume trading. It stops on an
ambiguous Railway mutation; inspect deployment state before another request.
Exchange fills after the snapshot must be reconciled before declaring recovery.
The rollback command verifies the local archive before entering maintenance,
uploads it to the same service only when rollback is invoked, restores the scoped
roots and retains an operator pause. The 43-file local drill passed. A production
restore drill and 24-hour acceptance remain unclaimed.

## Snapshot compatibility regression and containment

Deployment `6931d8f6-0072-4d5f-b5f1-26012028a17d` (commit `d8f34e7`, binary
`5980da752be99332ea658f0bef2860d73f6a9feb0378072552203f96b4a13b98`) introduced
an observer-snapshot serialization regression. An empty optional recovery field
became mandatory during reserialization, changing the retained ledger wire from
three fields to four. Strict canonical comparison correctly rejected it before
account reconciliation or order submission. Local tests had covered current
round trips but missed the deployed predecessor's omitted-field layout.

The supervisor logged 669 exit-1 failures before maintenance deployment
`0f4f76a3-ef76-4137-b780-1faa12f1dcad` stopped the loop on September 16 at about
18:42 UTC. These are real availability failures, not a successful run. No
acceptance window started. The previous supervisor incorrectly gave every
replacement a new 900-second boot budget, allowing an indefinite crash loop.
The repaired supervisor shares that budget across failed replacements, emits
`recovery_budget_exhausted`, and keeps control telemetry online without further
child launches until a repaired release is deployed.

The compatibility repair retains the exact old empty-field representation and
stores ownership tombstones in the existing optional recovery extension. The
actual 2,874,847-byte predecessor snapshot, generation 3128, passes strict wire
and original checksum verification, followed by both full accounting replay
passes. Checksums and canonical equality checks were not relaxed. A regression
test covers the exact three-field predecessor layout and rejects a changed
checksum. The failed deployment's complete retained diagnostics were captured
off-volume before the next release.
